type Props = { value?: string[]; onChange?: (v: string[]) => void; [key: string]: any };
export function AdditiveSelector(_props: Props) {
  return <div className="text-muted small">Selector aditivi indisponibil temporar.</div>;
}
export default AdditiveSelector;
