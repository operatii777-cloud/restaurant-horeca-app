# Core dependencies (always required)
requests>=2.28.0

# PDF text extraction (preferred, handles text-based PDFs without OCR)
pdfplumber>=0.9.0

# Image OCR (required for scanned PDFs and image files)
Pillow>=9.0.0
pytesseract>=0.3.10

# Convert PDF pages to images for OCR (required for scanned PDFs)
pdf2image>=1.16.0
